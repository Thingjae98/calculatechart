# CLAUDE.md - CalculateChart 개발 컨텍스트

## 프로젝트 개요

한국 주식(KRX) 차트 기술적 분석 + 점수화 + N일 예측 웹 앱.
개발자(명재)가 **컴퓨터를 잘 모르는 가족(부모님, 누나)**에게 제공하기 위한 개인 프로젝트.

---

## ⚠️ 핵심 설계 원칙 — 비전문가 사용자 우선

> 이 앱의 사용자는 주식 차트를 처음 보는 엄마, 누나다.
> 기능이 많아도 화면이 복잡하면 쓸 수 없다.
> **단순함이 기능보다 항상 우선이다.**

### 반드시 지켜야 할 UI 원칙

1. **차트에 선은 최소화**: 동시에 보이는 수평선(지지/저항/이평선 등)은 **5개 이하**로 유지
2. **전문 용어 금지**: "RSI", "MACD", "다이버전스" 같은 용어는 UI에 절대 노출하지 말 것. 점수, 신호 카드에서는 쉬운 말로만 설명
3. **색상 의미를 직관적으로**: 초록=좋다, 빨강=위험, 노랑=관망. 이 외의 색상 의미 혼용 금지
4. **클릭 한 번으로 결론**: "지금 사도 되나요?"에 대한 답이 첫 화면에 바로 보여야 함
5. **기능 추가 전 항상 질문**: "엄마가 이걸 보고 뭘 해야 하는지 알 수 있나?" — YES이면 추가, NO이면 숨기거나 제거

### 복잡한 지표는 백엔드에서만 처리
스토캐스틱, 다이버전스, 일목균형표, 피보나치 등은 **점수 계산에만 활용**.
차트에 직접 표시하면 오히려 혼란을 주므로 시각화는 신중하게 결정.

---

## 아키텍처

- **프론트엔드**: React 19 + TypeScript + Vite. `lightweight-charts` v5로 캔들/거래량/이평선 렌더링.
- **백엔드**: FastAPI (Python). `pykrx`로 KRX OHLCV 데이터 취득, `scipy`로 지지/저항선 탐지.
- **배포**: Render.com 무료 플랜 (cold start 이슈 → `/api/ping` 웜업으로 대응). 프론트엔드는 Vercel.

## 핵심 파일

| 파일 | 역할 |
|------|------|
| `backend/main.py` | 전체 백엔드 (엔드포인트, 기술적 분석, 예측 캔들, 추천) |
| `frontend/src/App.tsx` | 메인 UI (검색 자동완성, 예측 버튼, 추천 목록) |
| `frontend/src/components/CandleChart.tsx` | 차트 컴포넌트 (캔들+거래량+이평선+예측+스크롤 로드) |
| `frontend/src/lib/api.ts` | API 클라이언트 + TypeScript 타입 정의 |
| `frontend/src/App.css` | 전체 UI 스타일 (드롭다운, 카드, 차트) |
| `frontend/src/index.css` | CSS 변수 (다크모드/라이트모드 테마) |
| `frontend/index.html` | HTML 엔트리 (탭 타이틀: "차트 분석") |

## 개발 명령어

```bash
# 백엔드 (반드시 먼저 실행)
cd backend && uvicorn main:app --reload --port 8000

# 프론트엔드 (Vite 프록시가 /api/* → localhost:8000 전달)
cd frontend && npm run dev

# 빌드
cd frontend && npm run build
```

## 배포 설정

- **백엔드**: `render.yaml` → Render.com 자동 배포
- **프론트엔드**: Vercel에 `VITE_API_BASE_URL` 환경변수 필수 설정
- 로컬 개발 시 `VITE_API_BASE_URL`은 비워두면 Vite 프록시 사용

## 주의 사항

- `pykrx`는 동기 라이브러리 → `ThreadPoolExecutor`로 감싸서 비동기 호출 + 타임아웃 적용
- `pandas_ta`는 설치 실패 가능 → import 실패 시 수동 RSI 계산으로 폴백 (Wilder's EMA 방식 사용)
- 종목 리스트(`_load_listing`)는 fdr 우선 + pykrx 폴백, 1시간 캐시 + startup 사전 로드
- 추천 종목은 1시간 메모리 캐시. 캐시 없으면 220종목 순회하므로 수 분 소요
- CandleChart에서 `freshLoadId` prop으로 신규 로드 vs 스크롤 추가 로드를 구분
- 예측 캔들은 프론트에서 별도 CandlestickSeries(반투명)로 렌더링
- OHLCV 데이터에서 NaN/0값 행은 백엔드+프론트엔드 양쪽에서 필터링

---

## 🎨 차트 복잡도 문제 분석 및 해결 방향 (✅ 해결됨, commit 9e6d51e)

### 과거 문제: 최대 17개 수평선이 동시에 표시됨 → **현재는 기본 4선 이하로 축소**

| 레이어 | 개수 | 현재 상태 |
|--------|------|---------|
| 이동평균선 (5/20/60/120일) | 4선 | 항상 표시 |
| 지지선 / 저항선 | 2선 | 항상 표시 |
| 박스 상단/하단 | 2선 | 박스권 진입 시 표시 |
| 피보나치 레벨 | 5선 | 항상 표시 ← **즉시 제거 필요** |
| 일목균형표 (전환/기준/구름上下) | 4선 | 항상 표시 ← **즉시 제거 필요** |

비전문가가 보기에 선이 17개면 **무엇을 봐야 할지 알 수 없다**.

### 해결 방향: 기본 뷰 단순화 + 토글 방식

#### 기본 뷰 (항상 표시, 최대 5선)
| 표시 항목 | 이유 |
|---------|------|
| 캔들 + 거래량 | 필수 |
| **20일 이평선** (주황) | 한국 주식 방송에서 "20일선"으로 가장 많이 언급 |
| **60일 이평선** (파랑) | "2개월 추세선"으로 직관적 |
| 지지선 1개 (파랑 점선) | "여기서 버팀" — 의미 직관적 |
| 저항선 1개 (빨강 점선) | "여기서 막힘" — 의미 직관적 |

#### 숨김 처리 (토글로 ON/OFF)
| 숨길 항목 | 대안 |
|---------|------|
| 5일 / 120일 이평선 | "상세 이평선" 토글 버튼 |
| 피보나치 5선 | "전문가 모드" 토글 또는 완전 제거 |
| 일목균형표 4선 | 백엔드 점수 계산에만 활용, 차트 표시 제거 |
| 박스 상단/하단 | 박스권일 때만 표시 (현재 유지) |

#### 구현 방법 (CandleChart.tsx)
```tsx
// 상태 추가
const [showAllMA, setShowAllMA] = useState(false)
const [showAdvanced, setShowAdvanced] = useState(false)

// MA 렌더링 조건
// showAllMA=false: 20일, 60일만 표시
// showAllMA=true: 5일, 20일, 60일, 120일 모두

// 피보나치: showAdvanced=true일 때만 표시
// 일목균형표: 차트에서 완전 제거 (점수에만 활용)
```

#### 차트 위 토글 버튼 예시 (App.css와 통일)
```
[20일/60일 ✓]  [전체 이평선]  [피보나치]
```
버튼 스타일은 기존 `predBtn` 클래스와 동일하게 통일.

### 즉시 적용 우선순위
1. **피보나치 5선 → 기본 숨김** (토글로 선택)
2. **일목균형표 4선 → 차트에서 제거** (점수 계산만 유지)
3. **5일/120일 이평선 → 기본 숨김** (토글로 선택)
4. 나머지 (지지/저항/박스/20일/60일)는 유지

---

## 예측 모델 핵심 로직 (v3 — 몬테카를로 앙상블)

`_generate_predicted_candles()` 함수 (backend/main.py):

- **몬테카를로 앙상블**: 50개 경로 생성 → 중앙값으로 대표 캔들 도출
- **ADX + Hurst 지수**: 레짐(추세장/횡보장) 이중 판별
- **멀티타임프레임 모멘텀**: 5d/10d/20d/60d 가중 혼합
- **피보나치 자석 효과**: 0.382/0.618 레벨 내부 인력 적용 (차트 미표시)
- **적응형 GARCH**: 레짐별 변동성 파라미터 조정
- **앙상블 집계**: 고가=60th 퍼센타일, 저가=40th, 시/종가=중앙값
- **seed 결정론적**: `(last_close * 100 + date_hash)` 기반 — 같은 종목/날짜면 동일한 경로

## 지지/저항선 탐지 (v2)

- 다중 타임프레임(5/10/20봉) 피봇 고·저점 수집
- 터치횟수(40%) + 거래량집중도(30%) + 최근성(20%) + 현재가근접도(10%) 점수화
- 유효 범위: 현재가 위/아래 1%~20% 이내
- 가장 유의미한 1개씩만 최종 선택

## 수렴→돌파→눌림목 패턴 감지

- **돌파**: 최근 10일 내 BB 상단/박스 상단 돌파 + 거래량 20일 평균 1.8배 이상
- **눌림목**: 돌파 레벨 -3%~+5% 이내 되돌림 + 거래량 감소 → structure_pts +7
- **예측 반영**: 눌림목 구간에서 저항 반발력 1/3로 완화

## 커스텀 지표 설명

- **수박지표**: BB 스퀴즈 + 이평선 수렴(5/20/60/224일 편차 < 2%) 동시 발생 → 추세 전환 임박 신호
- **계단지표**: ATR×0.5 그리드 계단화. 3회+ 유지 + 상향 시 상승 확인

## 캔들 패턴 인식 (스코어링 반영, 최근 3봉)

| 패턴 | 점수 | 조건 |
|------|------|------|
| 망치형 🔨 | +3 | 전일 음봉 + 아래꼬리>몸통×2 |
| 역망치형 🔼 | +2 | 전일 음봉 + 위꼬리>몸통×2 |
| 도지 ⚖️ | 0~+2 | 몸통<ATR×0.1 |
| 상승장악형 🟢 | +3 | 전일 음봉을 양봉이 완전히 덮음 |
| 하락장악형 🔴 | -3 | 전일 양봉을 음봉이 완전히 덮음 |
| 모닝스타 🌟 | +5 | 큰음봉 + 소형봉 + 큰양봉 (3봉) |
| 이브닝스타 🌙 | -5 | 큰양봉 + 소형봉 + 큰음봉 (3봉) |
| 잠자리형 도지 🐉 | +3 | 긴 아래꼬리 + 도지 |
| 비석형 도지 🪦 | -3 | 긴 위꼬리 + 도지 |

## 점수 시스템 구조 (0-100, 기준 50) — v4 (전면 리빌딩)

| 카테고리 | 캡 | 주요 구성요소 |
|--------|-----|------------|
| 추세 | ±15 | MA 정렬, Cup 패턴, 골든크로스, **일목균형표** |
| 모멘텀 | ±12 | RSI, MACD, 이격도, **스토캐스틱**, 다이버전스 |
| 변동성 | ±10 | BB 위치, 수박지표(스퀴즈) |
| 거래량 | ±15 | 바닥 매집, OBV 다이버전스, 스퀴즈+거래량 폭발 |
| 구조 | ±8 | 지지/저항, 박스권, 계단지표, 눌림목, 캔들 패턴 |
| **위치(NEW)** | **±15** | **52주 가격 위치 + 최근 20일 성과 + 외인·기관 수급** |
| 반등 시너지 | +3~5 | 반등 플래그 2개+ 동시 발생 |

**이론상 최대합산 ~147점 → 100으로 클램핑됨**
**모든 지표는 점수에만 반영 — UI 신호 카드 문구는 반드시 쉬운 말로**

### 위치(position) 카테고리 상세 (v4 신규)
- **52주 가격 위치**: 연중 최고점 90%↑ → -12, 75%↑ → -6 / 연저점 10%↓ → +12, 25%↓ → +6
  - 근거: baseline 백테스트 — 고득점(75+) 종목이 연중 고점 근처에서 -0.92% 손실. 평균회귀 대상 식별
- **20일 수익률 (단기 과열)**: +25%↑ → -12, +15%↑ → -7 / -20%↓ → +10, -12%↓ → +5
- **외인·기관 수급**: 5일 순매수 500억↑ → +10, 100억↑ → +5 / 순매도 500억↓ → -10 (D항목 완료)
  - pykrx `get_market_trading_value_by_date()` 기반 / 추천 스캔에서는 제외(속도)

### 예측 모델 상향 편향 보정 (v4)
- `score_drift` 계수: 0.003 → **0.0012** (60% 축소)
- `momentum_drift`: 강추세 0.15 → **0.08**, 추세장 0.08 → **0.05**
- 근거: 7일 예측 상향 편향 +1.67% → 0%대로 수렴 기대

## 스토캐스틱 오실레이터 (14-3-3, 점수 전용)

- `%K` = 14일 범위 내 현재 위치, 3일 평활화 / `%D` = %K의 3일 평균
- RSI + 스토캐스틱 이중 과매도 → +5점 (강한 반등), 이중 과매수 → -5점
- %K가 %D 하향 돌파 + 바닥권: +3점 / 상향 돌파 + 고점권: -2점
- **차트에 표시하지 않음** — 점수 및 신호 문구에만 반영

## 일목균형표 (점수 전용)

- 전환선(9일), 기준선(26일), 선행스팬1·2, 구름(Kumo) 계산
- 구름 위 + 전환>기준 + 양운: +4점 / 구름 아래: -2~-4점
- **차트에 전환선/기준선/구름 선 현재 표시 중 → 기본 숨김으로 전환 예정**
- 스코어링에는 계속 활용

## 피보나치 되돌림 (점수 전용)

- 최근 120일 swing 고점/저점 기준 5레벨 (0.236~0.786) 계산
- 예측 모델 내 자석 효과에 활용, 스코어링에도 간접 반영
- **차트에 5선 현재 표시 중 → 기본 숨김으로 전환 예정**

## 다이버전스 감지 (점수 전용, find_peaks 기반)

`scipy.signal.find_peaks(distance=8, prominence=0.3σ)` 로 실제 피크 쌍 탐색:
- RSI 상승 다이버전스: +5점 / RSI 하락: -4점
- MACD 상승 다이버전스: +4점 / MACD 하락: -4점
- OBV 다이버전스: +8점
- **화면 노출 없음** — 신호 카드에 쉬운 문구로만 표시

## 추천 종목 필터링

- 시가총액 ≥ 500억 + 거래대금 ≥ 20억 (유동성 확보)
- 1시간 캐시, 220종목 샘플링

## 설계상 결정 (의도적)

- **예측 seed 결정론적**: 같은 종목/날짜 → 같은 예측 (재현성 확보)
- **SMA20 평균회귀**: `mean_revert += -dist_sma20 * 0.10` (올바른 방향)
- **스크롤 추가 로드**: OHLCV 데이터만 반환, fibonacci/ichimoku 미계산

---

## 🚧 작업 진행 상황

### ✅ P0 — 차트 단순화 (완료, commit 9e6d51e)
1. ✅ **피보나치 5선 기본 숨김** — `showFibonacci` 토글로 이동 (CandleChart.tsx)
2. ✅ **일목균형표 4선 차트에서 제거** — 백엔드 점수 계산에만 활용
3. ✅ **5일/120일 이평선 기본 숨김** — `showAllMA` 토글
4. ✅ **토글 버튼 UI 추가** (`chartToggles` CSS, predBtn 스타일)

**기본 차트 구성**: 캔들 + 거래량 + MA20(주황) + MA60(파랑) + 지지선 + 저항선 (최대 4개 수평선)
**토글 가능 항목**: 이평선 전체(5/20/60/120), 피보나치 5선

### ✅ P1-6 — 컵 패턴 슬라이딩 윈도우 (완료, commit 73715e0)
- 기존: 고정 60봉 + trough_idx ∈ [10,50] → 경계 바깥 패턴 누락
- 개선: 30/40/50/60/70/80봉 6개 윈도우 전체 훑기
- 품질 점수 = 회복률(0.40) + 깊이(0.20) + 대칭성(0.25) + 거래량확인(0.15)
- 양끝 15% 내 바닥 제외 (실제 '그릇' 모양만 인정)

---

## 🔬 백테스트 인프라 + 예측 정확도 분석 (진행 중)

### ✅ 백테스트 스크립트 구축 (완료, commit dcbfe52)

**파일**: `backend/backtest.py` (독립 스크립트, API 아님 — Render 배포 영향 없음)

**구조**:
- 종목당 히스토리 1회 로드 (2년) → 슬라이싱으로 N회 walk-forward 검증
- `_unified_score()` + `_generate_predicted_candles()` 직접 호출
- 결과 JSON 저장 (`backend/backtest_results/{timestamp}_{label}.json`, gitignore)

**측정 지표**:
- **MAPE** / Median MAPE / Bias / RMSE — 예측 정확도
- **Directional Accuracy** — 상승/하락 방향 맞춘 비율
- **점수 구간별 실제 수익률** — 점수 시스템 예측력 직접 검증

**실행 방법**:
```bash
cd backend
python backtest.py                              # 기본 10종목 180일
python backtest.py --label baseline             # 태그 지정
python backtest.py --tickers 005930,000660      # 특정 종목
python backtest.py --horizons 1,3,7,14 --step 5 # 예측 기간/간격
```

**기본 샘플 종목**: 005930(삼전), 000660(SK하이닉스), 035420(NAVER), 035720(카카오), 005380(현대차), 051910(LG화학), 006400(삼성SDI), 068270(셀트리온), 207940(삼바), 005490(포스코)

### 📊 Baseline 결과 (commit dcbfe52, 1080건 예측)

**Horizon 정확도**:
| Horizon | MAPE | Bias | Direction Acc. |
|---------|------|------|---------------|
| 1일 | 3.08% | +0.50% | 51.1% |
| 3일 | 5.22% | +1.18% | 49.7% |
| 7일 | **7.92%** | **+1.67%** | **49.2%** |

**🔴 점수 시스템 역작동 발견 (7일 기준)**:
| 점수 구간 | 실제 7일 수익률 | 상승확률 | 샘플 |
|----------|--------------|---------|-----|
| **75+ 강한 매수** | **-0.92%** | **23.5%** | 17 |
| 60-75 매수 | +2.08% | 50.4% | 127 |
| 50-60 관망상 | +1.33% | 50.7% | 144 |
| **35-50 관망하** | **+3.64%** | **59.2%** | 71 |
| <35 매도 | +3.74% | (1건) | 1 |

**핵심 문제**:
1. 예측 방향성이 50% 미만 (동전 던지기 수준)
2. 상향 편향 +1.67% (예측이 과도하게 긍정적)
3. **점수가 높을수록 실제 수익률이 낮음** (평균회귀가 모멘텀보다 강함)

### ⚠️ Score Fix 시도 결과 (commit 2a894bd — 효과 미미)

**변경 내용** (`_unified_score()`):
1. 이격도 ±10% 구간 추가 (+7/-8점)
2. RSI 70-75 중간 구간 (-6점), RSI 80+ 극단 (-10점)
3. 골든크로스 신선도 감쇠 (`days_since_golden_cross` 추적):
   - 0~1일: +12점 / 2~3일: +8점 / 4일+: +3점
4. 복합 경고 (RSI70+ & 이격+10% → -4 추가) / 복합 반등 (RSI30- & 이격-10% → +3 추가)

**결과 비교 (After vs Baseline)**:
| 지표 | Baseline | After Fix | 변화 |
|------|----------|-----------|-----|
| 1일 MAPE | 3.08% | 3.05% | ≈0 |
| 7일 MAPE | 7.92% | 7.91% | ≈0 |
| 7일 Direction Acc | 49.2% | 49.2% | 0 |
| 75+ 수익률 | -0.92% | **-1.51%** | 악화 |
| 35-50 수익률 | +3.64% | +3.43% | 비슷 |

**→ 결론**: 예측 모델 자체는 score_drift(아주 작은 가중치)로만 영향 받으므로 MAPE/Direction 변화 없음. 점수 시스템 튜닝만으로는 역상관 해소 불가. 근본적 접근 필요.

### 🎯 전면 리빌딩 완료 항목 (v4)

**B. 시장 지수(KOSPI) 영향 차감 — 알파/베타 분해** ✅ **완료 (commit efd7b27)**

**C. 예측 캔들에 지지/저항 반영** ✅ **이미 구현됨 (sr_levels 자석 효과, commit efd7b27 확인)**
- `_generate_predicted_candles` 내 `sr_levels` 리스트에 지지/저항선 포함
- 피보나치 자석 효과와 동일한 방식으로 이미 반발력 적용 중

**A. 샘플 확대** ✅ **완료 (backtest.py DEFAULT_TICKERS 10→32종목 확장)**
- 대형기술/반도체, 자동차, 에너지/화학, 바이오, 철강, 금융, 소비재, 엔터, 건설/방산 섹터 포함
- 예상 검증 건수: 32종목 × 180일 × 4 horizon ≈ 3,456건 이상

**D. 외인/기관 수급 추가** ✅ **완료 (v4 전면 리빌딩)**
- `_fetch_investor_flow_sync()` / `_fetch_investor_flow()` 추가
- `_unified_score(investor_flow=...)` 옵셔널 파라미터로 통합
- 5일 순매수 500억↑ → +10점, 100억↑ → +5점 (역순으로 감점)
- `/api/stock/{ticker}` + `/api/predict` 병렬 조회 (`asyncio.gather`)
- 추천 스캔(220종목)에서는 제외 — 속도 문제

**E. 점수 시스템 근본 개선 (v4 신규)** ✅ **완료**
- `position_pts` 신규 카테고리 (cap ±15) 추가:
  - 52주 가격 위치: 연고점 90%↑ → -12, 연저점 10%↓ → +12
  - 20일 수익률 과열: +25%↑ → -12, 급락 -20%↓ → +10
  - 외인·기관 수급 통합
- **의도**: baseline 백테스트에서 확인된 "고득점 = 연중 고점 = 평균회귀"를 score에 직접 반영
- 기대 효과: 75+ 점수대 종목이 자연스럽게 연중 저점 종목으로 이동

**F. 예측 상향 편향 보정 (v4 신규)** ✅ **완료**
- `score_drift` 계수: 0.003 → 0.0012
- `momentum_drift`: 강추세 0.15→0.08, 추세장 0.08→0.05

### 📊 v4 백테스트 결과 (동일 조건: 10종목, 180일, step=5, 1080건)

| 지표 | Baseline | Alpha/Beta | **v4 (전면 리빌딩)** | 변화 |
|------|----------|-----------|---------------------|-----|
| 1일 MAPE | 3.08% | 3.09% | **2.97%** | ↓ |
| 7일 MAPE | 7.92% | 7.78% | **7.73%** | ↓ |
| 1일 Bias | +0.50% | — | **+0.27%** | ↓ 46% |
| 3일 Bias | +1.18% | — | **+0.67%** | ↓ 43% |
| 7일 Bias | +1.67% | +1.40% | **+1.22%** | ↓ 27% |
| **1일 DirAcc** | 51.1% | — | **55.0%** | ↑ +3.9pp ✅ |
| 7일 DirAcc | 49.2% | 49.4% | 48.9% | ≈ |
| **75+ 수익률** | -0.92% | -1.51% | **-2.15%** | 악화 ⚠️ |
| 50-60 수익률 | +1.33% | — | **+2.16%** | ↑ |
| <35 수익률 | +3.74% | — | **+3.84%** | ↑ |

**핵심 해석**:
1. ✅ **편향 크게 개선**: 1일 +0.50%→+0.27%, 3일 +1.18%→+0.67%, 7일 +1.67%→+1.22%
2. ✅ **1일 방향정확도 55%** (기존 51%) — 단기 예측 개선
3. ⚠️ **75+ 역상관 구조 유지**: -2.15%로 오히려 악화
   - 원인: position_pts가 "연중 고점 모멘텀" 종목을 75+ 밖으로 밀어냄
   - 남은 75+ 종목: "회복 초기 단계 바닥 종목" (Cup + 52주 저점)
   - 이 종목들이 단기 7일엔 아직 반등 미확정 → 음수 수익
4. **최적 점수 구간 (7일): 50-60** (+2.16%, 57.9% 상승확률)
   - 기존 추천 레이블 재검토 필요

**다음 단계**: ~~75+ 점수대를 "단기 주의" 레이블로 변경 또는 60-75를 최적 매수 구간으로 표시~~ → 완료 (현재 레이블: "강한 추세 (단기 주의)")

### 📊 v4 중기(14/21/30일) 백테스트 결과 (10종목, 180일, step=5, 1080건)

**실행 명령**: `python backtest.py --tickers 005930,...,005490 --label v4_midterm --horizons 14,21,30 --step 5`

| Horizon | MAPE | Median MAPE | Bias | Direction Acc. |
|---------|------|------------|------|---------------|
| 14일 | 10.22% | 8.18% | **-4.19%** | 50.3% |
| 21일 | 11.74% | 9.00% | **-5.78%** | 49.7% |
| 30일 | 14.00% | 11.11% | **-7.68%** | 49.2% |

**30일 점수 구간별 실제 수익률 (핵심 발견)**:
| 점수 구간 | 평균 30일 수익률 | 상승 비율 | N |
|----------|----------------|---------|---|
| 75+ 강한 추세 | **+6.91%** | 68.8% | 16 |
| 60-75 매수 고려 | **+11.00%** | 69.2% | 104 |
| 50-60 관망 | **+12.19%** | 66.3% | 92 |
| 35-50 약세 주의 | +7.79% | 56.9% | 109 |
| <35 매도 권고 | +5.56% | 53.8% | 39 |

**핵심 해석**:
1. **75+ 점수, 단기→중기 전환 시 양수 수익**: 7일 -2.15% → 30일 **+6.91%** (구조적 역상관 아님, 단순 "시간 필요")
2. **50-60 여전히 최고 수익** (+12.19%): 과열 없는 적정 추세 종목이 중기에도 가장 안정적
3. **예측 캔들 bias 역전**: 7일엔 상향편향 +1.22%, 30일엔 **하향편향 -7.68%** (장기 예측일수록 과도한 평균회귀 작동)
4. **Direction Acc. 50% 수준**: 예측 캔들의 방향성은 중기에서도 동전 던지기 수준 — 예측 캔들은 "참고용" 이상의 의미 없음

**결론 및 권장 사항**:
- 75+ 점수 유효성: **단기(7일) 부적합 / 중기(30일) 유효** (+6.91%, 68.8% 상승확률)
- 현재 레이블 "강한 추세 (단기 주의)" 유지 타당 — 단기 보유보다 중기(한 달) 보유 권장하는 UI 문구로 보완
- 예측 캔들은 단기(7일 이내)에서만 방향 참고, 중기에선 점수 기반 판단이 더 유효

### 💡 백테스트 워크플로우 (전문가적 개선 사이클)

```
1. 현재 모델로 baseline 실행 (--label baseline)
2. 개선안 구현
3. 동일 조건으로 재실행 (--label after_xxx)
4. JSON 비교하여 MAPE, Direction Acc, 점수 구간별 수익률 개선 여부 확인
5. 개선 확인되면 commit, 아니면 원상복구
```

**금지 사항**:
- 백테스트 없이 파라미터 "감으로" 수정 금지
- 샘플 크기가 작을 때(<500건) 결론 내기 금지 — 노이즈 묻힐 수 있음

---

## 📦 남은 P1/P2 작업 목록

### P1 — 기능 개선
- 5. OBV 차트 표시: 하단 보조 패널 (비전문가 우선 원칙 고려 — 신호 카드 우선)
- ~~6. 컵 패턴 슬라이딩 윈도우~~ ✅ 완료
- 7. 일목균형표 후행스팬: 시계열 shift 표시 (전문가 모드 전용 — 낮은 우선순위)
- ~~8. 외인/기관 순매수 연동~~ ✅ 완료 (v4)

### P2 — 장기 로드맵
- 9. 52주 신고가 돌파 감지 (이제 52주 위치는 점수에 반영됨, UI 노출은 선택)
- 10. 보조지표 패널 분리 (RSI/MACD/스토캐스틱) — 원칙상 화면 복잡도 증가
- 11. 주봉/월봉 지원
- 12. 점수 가중 평균 리팩토링 (147→100 클램핑 개선)
